package com.training.deviceoperation.deviceconnection;
// because the parameters to SNMP to connect is diffrent from Telnet & SSH
public enum SNMP_par {
	
	strIPAddress,
	community,
	iSNMPVersion,
	strOID,
	Value
}
